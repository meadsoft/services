import { Injectable } from '@nestjs/common';
// import { SQL } from 'drizzle-orm';
// import { IFilter } from '@meadsoft/common';

@Injectable()
export class FiltersService {
    // convertToSql(filter: IFilter): SQL[] {
    //     return [SQL.raw(filter.sql, ...filter.params)];
    // }
    // convertManyToSql(filters: IFilter[]): SQL[] {
    //     return filters.map((filter) => SQL.raw(filter.sql, ...filter.params));
    // }
}
